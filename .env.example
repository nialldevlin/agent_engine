# Example .env for local development
# Copy to `.env` and update values as needed.

# Ensure the repo root is on PYTHONPATH so tests/imports work when running from VS Code
PYTHONPATH=${workspaceFolder}

# Example runtime configuration (uncomment and set as needed)
# AGENT_ENGINE_CONFIG=configs/basic_llm_agent/agents.yaml
# LOG_LEVEL=DEBUG
